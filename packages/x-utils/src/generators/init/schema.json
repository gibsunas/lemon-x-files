{
  "$schema": "https://json-schema.org/schema",
  "$id": "Init",
  "title": "",
  "type": "object",
  "properties": {
    "directory": {
      "type": "string",
      "description": "",

      "x-prompt": "Where is the directory we should start in?"
    },
    "logUrl": {
      "type": "string",
      "description": "",
      "$default": "/hookers/sse/empty",
      "x-prompt": "Where are the server side events emitted? (URL)"
    },
    "name": {
      "type": "string",
      "description": "",
      "$default": "utilsLogger",
      "x-prompt": "What name should we use?"
    }
  },
  "required": ["directory"]
}
